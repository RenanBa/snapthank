
<% if current_user %>
<div class="user_name">
  <p><span><%= current_user.name %></span> | <%= link_to 'Log Out', logout_path, method: :delete %></p>
</div>
<% end %>

<div class="container">
  <% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>">
      <p class="flash"><%= value %></p>
    </div>
  <% end %>
</div>

<!-- <div class="admin">
<%# if current_admin %>
  <%#= link_to "Logout", admin_logout_path %>
<%# else %>
  <p>Log in as <%#= link_to "Admin", admin_login_path %></p>
<%# end %>
</div> -->
